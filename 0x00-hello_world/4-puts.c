#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 * Return: return 0 everytime
 */
int main(void)
{

	char displayed[52];

	strcpy(displayed, "\"Programming is like a building a multilingual puzzle");
	puts(displayed);
	return (0);
}
